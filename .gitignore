# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.so

# Profiling
*.pclprof

# IDE
.idea/
.vscode/
*.swp
*.swo

# Environment
.env
venv/
env/
.venv/

# Local backups / scratch
cdf_bootstrap/
tmp_*.py

# Test run artifacts (regenerated by stress_test_handrank.py on demand)
stress_test_handrank_results.csv
stress_test_handrank_summary.md

# Secrets — NEVER commit (contains live AWS credentials)
config.ini

# Licensed third-party jars (ProPokerTools p2.jar/ppt_base.jar etc.) — keep local only,
# never publish. Used only by dev-time differential tests + one-time ordering extraction.
java_files/

# Local-only legacy / AWS-coupled engine variants kept out of the public launch repo
legacy/

# OS
.DS_Store
Thumbs.db

# Superpowers brainstorm companion
.superpowers/

# Local agent settings (do not publish)
.claude/

# Browser-automation + demo screenshots
.playwright-mcp/
demo-*.png

# Shared recent-queries store (live data)
recent_chats.json

# Large legacy lookup dicts (not needed by the slim engine)
score_dict.pkl
category_dict.pkl
